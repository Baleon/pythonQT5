in computer graphical user interfaces, drag-and-drop is the action of clicking on a virtual object and dragging it
to different location or onto another virual object, in general, it can be used to invoke many




import sys
from PyQt5.QtWidgets import QWidget, QApplication


class Example(QWidget):
    def __init__(self):
        super(Example, self).__init__()
        self.initUI()

    def initUI(self):

        self.setWindowTitle('check box')

if __name__ == '__main__':
    app = QApplication(sys.argv)
    ex = Example()
    ex.show()
    sys.exit(app.exec_())